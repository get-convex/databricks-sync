# Convex Databricks Sync

Repository scaffold for a packaged Convex component that incrementally syncs
Databricks tables into Convex.

## Development

```sh
npm install
npm run build:codegen
npm test
npm run lint
npm run typecheck
```
