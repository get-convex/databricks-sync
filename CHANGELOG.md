# Changelog

## 0.1.1

- Declare `@databricks/sql` 1.17.0 as a peer dependency so consuming Convex
  apps install and externalize the tested driver version directly.
- Add an integration test for the Databricks connection, session, statement,
  fetch, and cleanup contract used by the sync client.

## 0.1.0

- Initial release.
